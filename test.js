let chiffre = [1, 2, 3, 5, 6, 23];
// Permet d'afficher la valeur de l'index choisis number.at(3);
// permet d'ajouter un eelement a la derniere position number.push(4);
// permet de supprimer le dernier elemet du tableau number.pop();
// pernmet de remplacer tout les element du tableau par  l'element choisis number.fill(1);
// permet de supprimer le premier element du tableau number.shift();
// the methode enables of reverse elements from the array number.reverse();
// the methode enables of put  a element at the first position  number.unshift(3);
// the methode enables of verify the existences a element in the array number.includes(6);
// the method enables of transform element of the array number.map((item) => 2 * item);
// the method enables of filter the elements an array  f = chiffre.filter((index) => index > 5);

// the reduce method enables to return the additional element from the array console.log(chiffre.reduce((prev, curr) => prev + curr, 0));

//let s = chiffre.toString();

/*chiffre.forEach((index) => {
  console.log(`${index}`);
}); permet de parcourir le tableau*/
const tab = new Array(chiffre.isArray);
Array.isArray();
