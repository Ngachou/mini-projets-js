/**
 *
 * @param {[Array]} tab
 * @returns tab
 */

const tableau = function aaa(tab) {
  const tab = new Array("chien", "chat");
  console.log(tab.push(1, 2, 2, 2, 3, 4, 5, 5));
  console.log(tab);
};
export default tableau;
